databaseChangeLog:
  # New-project baseline only. Existing databases that ran the old migration chain
  # must be backed up and recreated before using this changelog.
  - include:
      file: db/changelog/001-baseline-schema.yaml
  - include:
      file: db/changelog/002-baseline-seed-data.yaml
